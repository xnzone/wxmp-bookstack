<view class="container">
  <!-- 顶部 -->
  <view class="header">
    <block wx:if="{{!searchMode}}">
      <text class="title">bookstack</text>
      <view class="search" bindtap="onSearch">
        <image src="https://s2.loli.net/2024/12/13/H4nBZUAoFzfeRI2.png" mode="aspectFit"></image>
      </view>
    </block>
    <block wx:else>
      <view class="search-bar">
        <input
          class="search-input"
          value="{{searchKeyword}}"
          placeholder="搜索书籍..."
          placeholder-class="search-placeholder"
          focus="{{true}}"
          bindinput="onSearchInput"
          confirm-type="search"
        />
        <view class="search-cancel" bindtap="onSearchCancel">取消</view>
      </view>
    </block>
  </view>

  <!-- 特色功能轮播（搜索时隐藏） -->
  <swiper
    wx:if="{{!searchMode}}"
    class="feature-swiper"
    indicator-dots="{{featureCards.length > 1}}"
    indicator-color="rgba(0,0,0,0.15)"
    indicator-active-color="#c0392b"
    autoplay="{{featureCards.length > 1}}"
    interval="4000"
    duration="500"
    circular="{{featureCards.length > 1}}"
  >
    <swiper-item wx:for="{{featureCards}}" wx:key="id">
      <view class="feature-entry" bindtap="onFeatureTap" data-id="{{item.id}}">
        <view class="feature-bg">
          <view class="feature-char-row" wx:if="{{item.chars}}">
            <text class="feature-char" wx:for="{{item.chars}}" wx:for-item="c" wx:key="*this">{{c}}</text>
          </view>
          <image wx:if="{{item.icon}}" class="feature-icon" src="{{item.icon}}" mode="aspectFit"></image>
          <view class="feature-info">
            <text class="feature-title">{{item.title}}</text>
            <text class="feature-desc">{{item.desc}}</text>
          </view>
          <view class="feature-arrow">→</view>
        </view>
      </view>
    </swiper-item>
  </swiper>

  <!-- 书籍网格 -->
  <view class="book-grid">
    <view class="book-card" wx:for="{{displayItems}}" wx:key="id" bindtap="goToDetail" data-id="{{item.id}}">
      <block wx:if="{{item.imageError}}">
        <view class="book-image-placeholder">📖</view>
      </block>
      <view wx:else class="book-image-wrap">
        <image
          class="book-image"
          src="{{item.image}}"
          mode="aspectFill"
          lazy-load="{{true}}"
          binderror="onImageError"
          data-id="{{item.id}}"
        ></image>
      </view>
      <view class="book-info">
        <text class="book-title">{{item.title}}</text>
        <text class="book-date">{{item.date}}</text>
      </view>
    </view>
  </view>

  <!-- 空结果 -->
  <view class="empty-state" wx:if="{{searchMode && displayItems.length === 0}}">
    <view class="empty-icon">📚</view>
    <text class="empty-text">没有找到相关书籍</text>
    <text class="empty-hint">试试换个关键词吧</text>
  </view>

  <!-- 翻页（搜索时隐藏） -->
  <view class="pagination" wx:if="{{!searchMode}}">
    <view class="pagi-inner">
      <!-- 上一页 -->
      <view
        class="pagi-btn pagi-prev {{currentPage === 1 ? 'pagi-disabled' : ''}}"
        bindtap="prevPage"
        data-disabled="{{currentPage === 1}}"
      >
        <text class="pagi-arrow">‹</text>
      </view>

      <!-- 页码指示器 - 圆点进度 -->
      <view class="pagi-indicator">
        <view class="pagi-dots">
          <view
            class="pagi-dot {{index + 1 === currentPage ? 'active' : ''}}"
            wx:for="{{totalPages}}"
            wx:for-index="index"
            wx:key="*this"
          ></view>
        </view>
        <text class="pagi-text">{{currentPage}} / {{totalPages}}</text>
      </view>

      <!-- 下一页 -->
      <view
        class="pagi-btn pagi-next {{currentPage === totalPages ? 'pagi-disabled' : ''}}"
        bindtap="nextPage"
        data-disabled="{{currentPage === totalPages}}"
      >
        <text class="pagi-arrow">›</text>
      </view>
    </view>
  </view>
</view>
